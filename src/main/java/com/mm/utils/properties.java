package com.mm.utils;

import java.util.Properties;

public class properties {

    public static Properties prop;

    public properties() {
        /*
         * try {
         * 
         * prop = new Properties(); FileInputStream ip;
         * 
         * ip = new FileInputStream(System.getProperty("user.dir")+
         * "\\src\\main\\resources\\config.properties"); prop.load(ip); } catch
         * (FileNotFoundException e) { e.printStackTrace(); } catch (IOException
         * e) { e.printStackTrace(); }
         */
    }
}
